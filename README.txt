This project work with R packages. Therefore, an R version at least 4.2.2 is required to be installed along the following packages

# install.packages("tidytext")
# install.packages("topicmodels")
# install.packages("stm")
# install.packages("tsne")
# install.packages("writexl")
# install.packages("ldatuning")

Moreover, this script uses a personal account with a twitter keys and tokens that enable the API to perform the search. Keys can be generated
creating a developer account on twitter. Then use the .env file to incorporate your creadentials to the script and be able to complete run the script.